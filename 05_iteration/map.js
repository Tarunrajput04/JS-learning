const counting=[1,2,3]
// console.log(nums.map((num)=>num*10));
const myNum = counting
                .map((num)=>num*10)
                .map((num)=>num+1)
                .filter((num)=>num>11)

console.log(myNum);

              

